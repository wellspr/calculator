{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "copy": "(cd ./public && rm -rf *) && (cp -r ../build/* ./public)",
    "build": "(cd .. && rm -rf build) && (cd .. && npm run build) && (npm run copy)"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.1",
    "nodemon": "^2.0.19"
  }
}
